# ECE Crypt — ECC Encryption Web App

ECC encryption web app (React + Vite) with curve visualization, ECC point arithmetic (BigInt), and Firebase Auth/Firestore public-key lookup.

## Features
- ECC point arithmetic with JavaScript BigInt
- Curve tools + visualization
- Encrypt/Decrypt workflows (ECIES-style)
- Firebase Auth + Firestore public-key directory

## Run locally (Windows)
```bash
npm install
npm run dev
